<h1><%= @note.title %></h1>

<p><%= @note.content %></p>

<%= link_to "Edit", edit_note_path(@note) %>

<%= button_to "Delete",
              note_path(@note),
              method: :delete,
              data: { turbo_confirm: "Are you sure?" } %>

<%= link_to "Back to Notes", notes_path %>
