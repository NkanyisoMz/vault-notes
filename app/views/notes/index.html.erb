<h1>My Notes</h1>

<p>Welcome <%= current_user.email %></p>

<%= link_to "New Note", new_note_path %>

<ul>
  <% @notes.each do |note| %>
    <li>
      <strong><%= note.title %></strong>
      <br>
      <%= note.content %>
    </li>
  <% end %>
</ul>
